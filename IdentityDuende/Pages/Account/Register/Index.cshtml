@page
@model IdentityServer.Pages.Account.Register.IndexModel
@{
}



<div>
    <partial name="_ValidationSummary" />
    <div class="row">
        <div class="col-sm-12 col-lg-6 offset-lg-3">
            <div class="card">
                <div class="card-header">
                    <h2>Register</h2>
                </div>
                <div class="card-body">
@*                    <form asp-route="Register">
                        <input type="hidden" asp-for="Input.ReturnUrl" />
                        <div class="form-group">
                            <label asp-for="Input.Email"></label>
                            <input class="form-control" placeholder="email" asp-for="Input.Email" autofocus>
                        </div>
                        <div class="form-group">
                            <label asp-for="Input.Password"></label>
                            <input type="password" class="form-control" placeholder="password"
                                   asp-for="Input.Password" autofocus>
                        </div>
                        <div class="form-group">
                            <label asp-for="Input.RepeatPassword"></label>
                            <input class="form-control" placeholder="name" asp-for="Input.RepeatPassword" autofocus>
                        </div>

                        <div class="form-group">
                            <label asp-for="Input.RoleName"></label>
                            <select asp-for="Input.RoleName" class="form-control"
                                    asp-items="@(new SelectList(role_list))"></select>
                        </div>
                        <button type="submit" class="btn btn-primary" name="button" value="register">Register</button>
                    </form>
                    *@
                </div>
                <div class="card-footer">
                    <a asp-page="../Login/Index">Login</a>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="h-100 d-flex">
    <form id="registerForm" class="text-center form needs-validation " asp-page="/Account/Register/Index" style="width:330px; margin: auto; ">

        <h1 class="h3 mb-3 fw-normal">Please sign up</h1>
        <div>
            <div class="form-floating">
                <input type="email" class="form-control" placeholder="Email" asp-for="Input.Email" required>
                <label asp-for="Input.Email"></label>
                <div class="invalid-feedback my-2">
                    <span asp-validation-for="Input.Email"></span>
                </div>
            </div>
            <div class="form-floating">
                <input type="password" class="form-control" placeholder="Password" asp-for="Input.Password" autocomplete="off" required>
                <label asp-for="Input.Password"></label>
                <div class="invalid-feedback my-2">
                    <span asp-validation-for="Input.Password"></span>
                </div>
            </div>
@*            <div class="form-floating">
                <input type="password" class="form-control" placeholder="Password" asp-for="Input.RepeatPassword" autocomplete="off" required>
                <label asp-for="Input.RepeatPassword">Repeat Password</label>
                <div class="invalid-feedback mt-0">
                    <span asp-validation-for="Input.RepeatPassword"></span>
                </div>
                <span asp-validation-for="Input.RepeatPassword"></span>
            </div>*@
        </div>
        <button class="w-100 btn btn-lg btn-primary mt-3" name="Input.Button" value="login">Sign up</button>
        <p class="mt-2 mb-3 text-muted">
            &copy;
            <a href="https://www.linkedin.com/in/konrad-dzięcioł-235a8523a" class="link-primary"><i class="fa-brands fa-linkedin"></i></a>
        </p>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        (function () {
            'use strict'

            // Fetch all the forms we want to apply custom Bootstrap validation styles to
            var forms = document.querySelectorAll('.needs-validation')

            // Loop over them and prevent submission
            Array.prototype.slice.call(forms)
                .forEach(function (form) {
                    form.addEventListener('submit', function (event) {
                        if (!form.checkValidity()) {
                            event.preventDefault()
                            event.stopPropagation()
                        }

                        form.classList.add('was-validated')
                    }, false)
                })
        })()
    </script>
}